"use client";
export function ConnectWallet() {
  // Handling this section of the project was optional; I did not implement this feature because of time constraints.
  const buttonStyles =
    "bg-[#47FC28] text-[#000]  py-2 px-6 rounded-lg transition-all duration-300 ease-in-out  hover:shadow-lg hover:shadow-emerald-500/10 active:scale-95";

  return <button className={buttonStyles}>Connect Wallet</button>;
}
